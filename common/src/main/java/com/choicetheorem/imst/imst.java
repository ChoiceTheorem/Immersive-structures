package com.choicetheorem.imst;

public final class imst {
    public static final String MOD_ID = "imst";

    // We can use this if we don't want to use DeferredRegister
    public static void init() {
        //System.out.println(imstExpectPlatform.getConfigDirectory().toAbsolutePath().normalize().toString());
    }
}
